export const metadata = {
  title: "EVENTIA - Invitation",
  description: "Espace invite pour consulter l'invitation et echanger."
};

export default function InviteTokenLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
